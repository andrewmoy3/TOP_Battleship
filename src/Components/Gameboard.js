
import Ship from "./Ship"

export default function Gameboard(){

    const receiveAttack = function(){}
    const placeShip = function(){}
    const hasShipAt = function(){}

    return {
        receiveAttack,
        placeShip,
        hasShipAt,
    }
}